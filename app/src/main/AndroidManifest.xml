<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lastprojectbangkit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LastProjectBangkit">
        <activity
            android:name="com.example.lastprojectbangkit.home.DashboardActivity"
            android:exported = "false"/>
        <activity
            android:name="com.example.lastprojectbangkit.camera.CameraActivity"
            android:exported="false"/>
        <activity
            android:name="com.example.lastprojectbangkit.home.MainActivity"
            android:exported="false"/>
        <activity
            android:name="com.example.lastprojectbangkit.view.onBoarding.OnBoardingActivity"
            android:exported="false"/>
        <activity
            android:name="com.example.lastprojectbangkit.view.onBoarding.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>